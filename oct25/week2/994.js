/**
 * @param {number[][]} grid
 * @return {number}
 */
var orangesRotting = function(grid) {
    let elapsedMinutes;

    //assuming grid has fresh apples
    let hasFresh = true;

    //loop until grid has no more fresh apples
    while(hasFresh){
        
    }
};


console.log(orangesRotting([[2,1,1],[1,1,0],[0,1,1]]));