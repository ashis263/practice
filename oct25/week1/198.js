/**
 * @param {number[]} nums
 * @return {number}
 */
var rob = function (nums) {
  let totalMoney = 0;

  let i = 0;
  while(i<nums.length){
    
  }

  return totalMoney;
};

console.log(rob([2, 7, 9, 3, 1]));
